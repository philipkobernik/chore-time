// This is in JavaScript so that it's truly global -- the one global object we want
root = (typeof global !== "undefined" && global !== null) ? global : window;
root.ChoreTime = root.ChoreTime || {};

